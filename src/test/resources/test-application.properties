# Configuração do Hibernate
# spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect # não é necessário.
# spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.hibernate.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configuração de acesso à base de dados (driver, host, port e DB, user e passwd)
spring.datasource.url=jdbc:postgresql://172.26.27.13:5433/Credit_System_DB
spring.datasource.username=obdantonio
spring.datasource.password=VerTigO-165*!

# Configuração para a base de dados
spring.datasource.initialization-mode=always
spring.datasource.initialize=true
spring.datasource.schema=classpath:/schema.sql
spring.datasource.continue-on-error=true

spring.flyway.enable=false